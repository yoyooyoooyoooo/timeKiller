package com.example.interview.Notes;

import java.util.concurrent.ExecutorService;

public class N3 {
    /*

Handle exception:  add throws keyword  //  use try catch     both need define e  // can have user define exception

try with resource auto close closeable

Generic data type has to be objecte, cant be primitive // during data erase (runtime) Generic swap to object type
public static <A> A func name (A[] array)   //   class classname <A key, B val>{}
int[][]   the int[0] is an OBJECT

    */
     public static void test() {

     }
}
